# ⚠️ Supabase API Key 錯誤修復

## 問題

```
Forbidden use of secret API key in browser
```

## 原因

你使用了 **Secret API Key** (開頭為 `sb_secret_`)，但這只能在後端服務器上使用，不能在瀏覽器中使用。

## 解決方案

### Step 1: 找到正確的 Anon Key

1. 打開 [Supabase 控制面板](https://supabase.com/dashboard)
2. 選擇你的項目
3. 進入 **Settings** → **API**
4. 在 **Project API Keys** 區域中找到：
   - ❌ `secret` (紅色圖標) - **不能用於瀏覽器**
   - ✅ `anon` (綠色圖標) - **用於瀏覽器** ← 複製這個

### Step 2: 更新 .env.local

```dotenv
VITE_SUPABASE_URL=https://cwkeaqwtfauytohryamg.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGc...  # 複製 anon key，通常以 "ey" 開頭
```

### Step 3: 立即刪除舊的 Secret Key

由於你不小心暴露了 Secret Key，請：

1. 進入 Supabase → **Settings** → **API**
2. 在 Secret Key 旁邊點擊 **Rotate Key** 或 **Delete**
3. 確認刪除以保護你的數據庫

### Step 4: 重新測試

更新 .env.local 後，重新啟動開發服務器：

```bash
npm run dev
```

然後完成一局遊戲測試上傳是否成功。

## API Key 差異對比

| 項目       | Anon Key   | Secret Key      |
| ---------- | ---------- | --------------- |
| 用途       | 客户端應用 | 後端服務器      |
| 用於瀏覽器 | ✅ 可以    | ❌ 禁止         |
| 權限       | 受限制     | 完全權限        |
| 前綴       | `ey...`    | `sb_secret_...` |
| 可暴露     | 相對安全   | ❌ 必須保密     |

## 🔒 安全提示

- **Anon Key** 可以公開（因為權限受限）
- **Secret Key** 必須保密（永遠不要提交到 Git）
- 已暴露的 Secret Key 必須立即轉換/刪除
